# Cost of operations on lists (arrays)
# l = list

# l[i] = l[j] + 5 -> O(1)
# l.append(i) -> table doubling O(1)

# l = l1 + l2 -> O(1 + length_l1 + length_l2)

# x in l -> linear

# len(l) -> O(1)

# l.sort() -> O(n log n) * time to compare items / where n is length of the list


#### Dictionaries #####